import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs'

import { SkipToMain } from './SkipToMain'

<Meta title="documentation/SkipToMain" />

# Skip to main

The skip to main - also known as skip navigation links - is a way for the user to skip repeated content on a page, which usually is the navigation.
It is made to meet the [Succes Criteria 2.4.1 Bypass Blocks](https://www.w3.org/TR/WCAG21/#bypass-blocks)

## Usage

```javascript
import { SkipToMain } from 'wcag-react-components'
```

```javascript
<SkipToMain text="Click 'enter' to skip to main content" />
```

## Props

<ArgsTable of={SkipToMain} />

## Features

<table>
  <tbody>
    <tr>
      <th>Tab</th>
      <td>Skip to main is only visible when in focus or active. Use the tab key to focus the skip to main</td>
    </tr>
  </tbody>
</table>
